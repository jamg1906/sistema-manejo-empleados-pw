@model IEnumerable<SistemaManejoEmpleados.Models.Director>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dpi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bonos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dpi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bonos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Dpi">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Dpi">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Dpi">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
