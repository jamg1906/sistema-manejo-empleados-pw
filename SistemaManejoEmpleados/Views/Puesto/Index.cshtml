@model IEnumerable<SistemaManejoEmpleados.Models.Puesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombrePuesto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionPuesto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombrePuesto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionPuesto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPuesto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPuesto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPuesto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
